---
title: This is the test for syntax lightlight
description: This is the test for syntax lightlight
id: 961ed19e-5574-4513-a798-5d3ba6146223
date: 2022-Oct-21 10:55:10+08:00
category: Software-design
---

## This is the test for syntax lightlight

```tsx
const BlogPage = () => {
  return (
    <section className="mx-auto max-w-5xl">
      <h1 className="mb-5 font-sans-serif text-3xl font-bold">Blog</h1>
      {allPosts
        .sort((a, b) => {
          if (new Date(a.date) > new Date(b.date)) {
            return -1;
          }
          return 1;
        })
        .map((post) => {
          return (
            <Link
              key={post.id}
              className="mb-4 flex flex-col space-y-1 font-sans-serif "
              href={post.url}
            >
              <div className="flex w-full flex-col font-sans-serif text-3xl">
                <p>{post.title}</p>
              </div>
              <div className="font-sans-serif text-sm text-[#8e8e8e] ">
                <p>{post.date}</p>
              </div>
            </Link>
          );
        })}
    </section>
  );
};
```

## Some dujnasd code

hahahahah

`this is a code block`

## This is a call out 

<Callout emoji="🥸">

this is a callout block

</Callout>